# Super Evil brand fonts

These `.otf` files are **licensed brand assets** for Super Evil Sound web use only.

- Do not redistribute font files.
- Confirm your purchase/license covers `@font-face` embedding on the production domain before publishing a public repository that includes these files.
- If the repo is public and the license does not allow it, host fonts via your licensed webfont provider instead and remove the binaries from git.
